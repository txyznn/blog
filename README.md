# Chaowen 博客

一个使用 Next.js、Tailwind CSS 和 MDX 搭建的个人博客起点。

## 本地运行

```bash
npm install
npm run dev
```

文章放在 `src/content/posts` 下维护，当前主要使用 `.mdx`，后续将支持普通 `.md` 文章。生产环境已部署到自有服务器，相关记录见 `docs/05-deployment/自有服务器部署.md`。
