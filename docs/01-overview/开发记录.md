# 开发记录

用于记录由 AI 协助完成的功能开发、重构和验证工作。

## 2026-09-11

### 首页与组件化迁移

- 拆分首页 Hero 区域、侧栏和 Newsletter 订阅区域。
- 拆分主导航和主题切换组件，重构 `SiteHeader`。
- 保留客户端交互边界，精简 `HomeClient` 页面编排职责。
- 复用 shadcn/ui 的 `Button`、`Badge`、`Card` 和 `Input` 组件。

### 博客功能完善

- 新增文章搜索页面 `/search`，支持标题、摘要、分类和标签搜索。
- 接通顶部搜索入口。
- 新增 RSS 订阅地址 `/rss.xml`。
- 新增站点地图 `/sitemap.xml`。
- 保持首页、归档、分类、标签和文章详情页的静态生成能力。

### 验证结果

- `npx tsc --noEmit` 通过。
- `npm run build` 通过。
- 构建生成 17 个页面和路由。

### 后续待处理

- 配置 HTTPS 证书和自动续期。
- 接入真实 Newsletter 邮件服务。
- 替换 Hero、社交链接和关于页面中的占位链接。
- 配置 GitHub Actions 自动部署。
- 补充正式的 SEO 和社交分享图片元数据。
